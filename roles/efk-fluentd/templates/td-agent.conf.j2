<source>
  @type forward
  @label @TEST
  port 24224
  bind 0.0.0.0
</source>


<label @TEST>
  <filter>
    @type parser
    key_name log
    reserve_data true
    <parse>
      @type regexp
      expression /^(?<logcontent>\w+\s\w+)/
    </parse>
  </filter>
  <filter>
    @type record_transformer
    remove_keys source,container_id,log
    <record>
      tag ${tag}
      timestamp ${time}
    </record>
  </filter>
  <match **>
    @type forward
    <server>
      host {{ fluentd_server }}
    </server>
  </match>
</label>
